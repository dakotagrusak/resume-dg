language: node_js
node_js:
  - "node"
before_install:
  - npm cache verify
  - npm cache clean --force
  - npm install -g npm@latest
script:
  - npm run analyze
deploy:
  provider: script
  script: bash scripts/deploy.sh
  on:
    tags: true
    all_branches: false
