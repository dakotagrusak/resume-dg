language: node_js
node_js:
  - "node"
before_install:
  - npm cache verify
  - npm cache clean --force
  - npm install -g npm@latest
script:
  - npm run analyze
  - npm run compile
deploy:
  provider: script
  script: bash scripts/deploy.sh
  skip_cleanup: true
  on:
    tags: true
    all_branches: false
